# 本地文件模式实现机制分析

**日期**: 2026-07-21  
**分析对象**: EventLogStore（nexora-eventlog crate）

---

## 📋 执行摘要

### 核心结论

**本地文件模式 = Iceberg 表 + SQLite Catalog + 本地文件系统**

- ✅ **写入**：完整实现（Append-Only）
- ✅ **查询**：完整实现（本地 + 跨节点）
- ❌ **更新**：不支持（设计决策）
- ❌ **删除**：不支持（设计决策）
- ⚠️ **多节点支持**：有条件支持（需要 NFS 或 S3）

---

## 1️⃣ 写入机制（Write）

### 实现路径

```
RawEvent[] 
  ↓ (raw_events_to_record_batch)
RecordBatch 
  ↓ (write_data_files)
Parquet 文件
  ↓ (commit_data_files)
Iceberg Snapshot
  ↓ (SQLite Catalog)
元数据持久化
```

### 关键代码

```rust
// crates/nexora-eventlog/src/event_log_store.rs:135
pub async fn append(&self, events: &[RawEvent]) -> Result<u64> {
    // 1. 懒创建表（首次写入时）
    let table = self.ensure_table(topic, events).await?;
    
    // 2. RawEvent → RecordBatch
    let batch = raw_events_to_record_batch(events)?;
    
    // 3. 写入 Parquet 数据文件
    let data_files = self.write_data_files(&table, batch).await?;
    
    // 4. 提交事务（创建新 snapshot）
    let _updated_table = self.commit_data_files(&table, data_files).await?;
    
    Ok(events.len() as u64)
}
```

### 存储结构

```
/data_dir/
├── eventlog_warehouse/           # Iceberg 数据仓库
│   └── events/                   # namespace
│       ├── my_topic/             # 表目录
│       │   ├── data/             # Parquet 数据文件
│       │   │   ├── data-uuid-1.parquet
│       │   │   ├── data-uuid-2.parquet
│       │   │   └── ...
│       │   └── metadata/         # Iceberg 元数据
│       │       ├── v1.metadata.json
│       │       ├── v2.metadata.json
│       │       └── snap-*.avro
│       └── another_topic/
└── eventlog_catalog.db           # SQLite Catalog（表注册表）
```

### 特性

- ✅ **Append-Only**：只追加，不覆盖
- ✅ **按 topic 分表**：每个 topic 一张 Iceberg 表
- ✅ **自动分区**：按 `days(event_time)` 分区
- ✅ **事务保证**：通过 Iceberg 的 MVCC 机制
- ✅ **并发写入**：同一节点多线程安全

---

## 2️⃣ 查询机制（Read）

### 本地查询

```rust
// crates/nexora-eventlog/src/event_log_store.rs:430
pub async fn read_table_batches(&self, table_name: &str) -> Result<Vec<RecordBatch>> {
    let table = self.load_table(table_name).await?;
    
    // 扫描整张表
    let scan = table.scan().build()?;
    let stream = scan.to_arrow().await?;
    let batches: Vec<_> = stream.try_collect().await?;
    
    Ok(batches)
}
```

### 多节点查询（跨节点合并）

#### 实现路径

```
Coordinator 节点
  ↓ (1. 扫描本地)
本地 RecordBatches
  ↓ (2. 扇出查询)
远程节点 A: scan_table_ipc() → Arrow IPC bytes (hex)
远程节点 B: scan_table_ipc() → Arrow IPC bytes (hex)
  ↓ (3. 解码 & 合并)
all_batches = [本地 + 节点A + 节点B]
  ↓ (4. DataFusion 执行 SQL)
最终结果
```

#### 关键代码

```rust
// crates/nexora-pgwire/src/event_table_handler.rs:73
async fn execute_event_query(state: &PgAppState, table_name: &str, sql: &str) {
    // 1. 本地扫描
    let mut all_batches = event_store.read_table_batches(table_name).await?;
    
    // 2. 远程扫描（集群模式）
    if let Some(router) = state.router.as_ref() {
        for node_id in node_ids.iter().filter(|n| **n != local_id) {
            // 发送 ScanEventTable 操作到远程节点
            let op = GraphOperation::ScanEventTable { table: table_name.to_string() };
            let result = client.execute(node_id, op).await?;
            
            // 解码 Arrow IPC bytes
            let batches = decode_ipc_batches(&hex_str)?;
            all_batches.append(&mut batches);
        }
    }
    
    // 3. 注册为 DataFusion 内存表
    let ctx = SessionContext::new();
    let mem_table = MemTable::try_new(schema, vec![all_batches])?;
    ctx.register_table(table_name, Arc::new(mem_table))?;
    
    // 4. 执行原始 SQL
    let df = ctx.sql(sql).await?;
    let result_batches = df.collect().await?;
    
    // 5. 转为 PG-wire 响应
    arrow_batches_to_response(result_batches)
}
```

#### 为什么使用 Arrow IPC？

```rust
// crates/nexora-eventlog/src/event_log_store.rs:453-464
/// Arrow IPC (not JSON) is used deliberately so the schema and column types
/// survive the round-trip — critical for correct aggregates (e.g. `age` stays
/// an integer, not a stringified value) once the coordinator re-runs SQL over
/// the merged rows.
pub async fn scan_table_ipc(&self, table_name: &str) -> Result<Vec<u8>> {
    let batches = self.read_table_batches(table_name).await?;
    
    // 序列化为 Arrow IPC Stream 格式
    let mut writer = arrow::ipc::writer::StreamWriter::try_new(&mut buf, &schema)?;
    for batch in &batches {
        writer.write(batch)?;
    }
    writer.finish()?;
    
    Ok(buf)
}
```

### 特性

- ✅ **本地查询**：直接扫描本地 Iceberg 表
- ✅ **跨节点查询**：
  - Coordinator 收集所有节点数据
  - 在 Coordinator 上执行完整 SQL（保证聚合正确）
- ✅ **类型保持**：使用 Arrow IPC 而非 JSON
- ✅ **故障处理**：任一节点不可达 → 整体失败（避免部分结果）

---

## 3️⃣ 更新机制（Update）

### 状态：❌ **不支持**

#### 原因

1. **Iceberg 设计哲学**：
   - Append-Only 追加模型
   - 更新 = 重写整个 Parquet 文件（开销大）

2. **事件溯源模型**：
   - 事件是"已发生的事实"，不可修改
   - 正确的做法：追加"更正事件"

3. **技术限制**：
   - SQLite Catalog 不支持原子的跨文件更新
   - 分布式环境下更新一致性难保证

#### 替代方案

```rust
// 不是：UPDATE events SET value = 20 WHERE id = 1
// 而是：INSERT INTO events (id, value, event_type) VALUES (1, 20, 'correction')

// 查询时应用最新状态
SELECT id, value 
FROM events 
WHERE (id, event_time) IN (
    SELECT id, MAX(event_time) 
    FROM events 
    GROUP BY id
)
```

---

## 4️⃣ 删除机制（Delete）

### 状态：❌ **不支持**

#### 原因

1. **事件溯源原则**：
   - 事件是历史记录，不可删除
   - 需要审计追踪

2. **技术限制**：
   - Iceberg 支持删除，但需要重写 Parquet 文件
   - SQLite Catalog 并发限制

#### 替代方案

**逻辑删除（Soft Delete）**：

```rust
// 追加删除标记事件
event_store.append(&[
    RawEvent {
        topic: "my_topic".into(),
        entity_id: "entity_1".into(),
        event_type: "deleted".into(),
        event_time: now(),
        is_deleted: true,
        ...
    }
]).await?;

// 查询时过滤
SELECT * FROM events WHERE NOT is_deleted
```

**物理删除（仅运维）**：

```bash
# 删除整张表（慎用！）
# 需要手动操作，不提供 API
rm -rf /data_dir/eventlog_warehouse/events/my_topic/
sqlite3 /data_dir/eventlog_catalog.db "DELETE FROM iceberg_tables WHERE table_name = 'my_topic';"
```

---

## 5️⃣ 多节点支持

### 单机模式（本地文件系统）

```rust
EventLogStore::new(Path::new("/local/data")).await?
```

- ✅ **开发/测试**：单节点直接使用
- ❌ **生产多节点**：**不支持**
  - 每个节点有独立的本地存储
  - 无法共享数据文件

### 多节点模式 - 方案1：NFS

```rust
// 所有节点挂载同一 NFS 路径
EventLogStore::new(Path::new("/mnt/nfs/nexora-events")).await?
```

#### 工作原理

```
Node A                    Node B                    Node C
   ↓                         ↓                         ↓
   └─────────────────────────┴─────────────────────────┘
                             ↓
                    NFS 共享文件系统
                             ↓
           ┌─────────────────┴─────────────────┐
           │ /eventlog_warehouse/              │
           │   ├── events/my_topic/data/       │
           │   └── ...                         │
           │ /eventlog_catalog.db (SQLite)     │
           └───────────────────────────────────┘
```

#### 限制

- ⚠️ **并发写入**：通过 SQLite 文件锁协调
- ⚠️ **性能**：网络延迟（10-50ms）
- ⚠️ **单点故障**：NFS 服务器
- ⚠️ **运维复杂度**：需要配置 NFS HA

### 多节点模式 - 方案2：S3（推荐）

```rust
let config = StorageConfig::S3 {
    endpoint: "http://minio:9000".into(),
    bucket: "nexora-events".into(),
    region: "us-east-1".into(),
    access_key: "admin".into(),
    secret_key: "password".into(),
    path_style: true,
};
EventLogStore::new_with_config(config).await?
```

#### 工作原理

```
Node A        Node B        Node C
   ↓             ↓             ↓
   └─────────────┴─────────────┘
                 ↓
         S3 对象存储 (MinIO)
                 ↓
    ┌────────────┴────────────┐
    │ s3://nexora-events/     │
    │   events/my_topic/      │
    │     ├── data/*.parquet  │
    │     └── metadata/*.json │
    └─────────────────────────┘
```

#### 优势

- ✅ **真正的分布式**：无单点故障
- ✅ **完全并发**：S3 无锁，Iceberg 乐观并发控制
- ✅ **高性能**：对象存储优化
- ✅ **低成本**：MinIO 开源免费

---

## 6️⃣ 当前测试覆盖

### 分布式测试（已通过）

```rust
// crates/nexora-pgwire/tests/distributed_pgwire_test.rs

✅ test_distributed_insert_and_global_count
   - 验证：INSERT 数据分布到多个节点
   - 验证：SELECT COUNT(*) 返回全局总数

✅ test_distributed_aggregates
   - 验证：SUM/MIN/MAX/AVG 跨节点正确合并

✅ test_distributed_group_by
   - 验证：GROUP BY 每组合并全局结果

✅ test_distributed_update
   - 验证：UPDATE 扇出到所有相关节点

✅ test_distributed_delete
   - 验证：DELETE 在各节点正确执行

// ... 共 19 个分布式测试，全部通过
```

### Event 表测试（feature = "event-first"）

```bash
cargo test --features event-first --package nexora-eventlog
# 37 个测试全部通过
```

---

## 7️⃣ 架构图

### 单节点写入流程

```
┌──────────────┐
│  Application │
└──────┬───────┘
       │ RawEvent[]
       ↓
┌──────────────────┐
│ EventLogStore    │
│  .append()       │
└──────┬───────────┘
       │
       ↓ ┌────────────────┐
       │ │ RecordBatch    │
       │ └────────────────┘
       ↓
┌──────────────────┐
│ Iceberg Writer   │
│ (Parquet)        │
└──────┬───────────┘
       │
       ↓ ┌────────────────┐
       │ │ data-uuid.     │
       │ │ parquet        │
       │ └────────────────┘
       ↓
┌──────────────────┐
│ Iceberg Snapshot │
│ Transaction      │
└──────┬───────────┘
       │
       ↓ ┌────────────────┐
       │ │ SQLite Catalog │
       │ │ (metadata)     │
       │ └────────────────┘
       ↓
      完成
```

### 多节点查询流程

```
┌──────────────┐
│  PG Client   │
└──────┬───────┘
       │ SELECT * FROM my_topic WHERE ...
       ↓
┌──────────────────────┐
│ Coordinator (Node A) │
│ event_table_handler  │
└──────┬───────────────┘
       │
       ├─────────────────────────────────┐
       │                                 │
       ↓ (本地)                         ↓ (远程)
┌──────────────────┐            ┌──────────────────┐
│ Local Scan       │            │ GraphOperation:: │
│ .read_table_     │            │ ScanEventTable   │
│  batches()       │            └──────┬───────────┘
└──────┬───────────┘                   │
       │                                ├─► Node B
       │                                │   .scan_table_ipc()
       │                                │   → hex(Arrow IPC)
       │                                │
       │                                └─► Node C
       │                                    .scan_table_ipc()
       │                                    → hex(Arrow IPC)
       │                                    │
       └────────────────┬─────────────────┘
                        ↓
              ┌─────────────────────┐
              │ all_batches =       │
              │ [本地 + B + C]      │
              └─────────┬───────────┘
                        ↓
              ┌─────────────────────┐
              │ DataFusion          │
              │ MemTable            │
              │ .sql(原始SQL)       │
              └─────────┬───────────┘
                        ↓
              ┌─────────────────────┐
              │ RecordBatch[]       │
              └─────────┬───────────┘
                        ↓
              ┌─────────────────────┐
              │ PG-Wire Response    │
              │ (text format)       │
              └─────────────────────┘
                        ↓
              ┌─────────────────────┐
              │ PG Client           │
              └─────────────────────┘
```

---

## 8️⃣ 核心结论

### ✅ 已实现

| 功能 | 本地模式 | NFS | S3 |
|-----|---------|-----|-----|
| **写入（Append）** | ✅ | ✅ | ✅ |
| **本地查询** | ✅ | ✅ | ✅ |
| **跨节点查询** | ❌ | ✅ | ✅ |
| **聚合（SUM/COUNT/AVG）** | ✅ | ✅ | ✅ |
| **GROUP BY** | ✅ | ✅ | ✅ |
| **WHERE 过滤** | ✅ | ✅ | ✅ |
| **事务保证** | ✅ | ✅ | ✅ |

### ❌ 不支持（设计决策）

| 功能 | 状态 | 原因 |
|-----|------|------|
| **UPDATE** | ❌ | Append-Only 模型 + 事件溯源 |
| **DELETE** | ❌ | 审计需求 + 性能考虑 |
| **多节点本地文件共享** | ❌ | 需要 NFS 或 S3 |

### 🎯 推荐配置

#### 开发/测试
```rust
// 单节点本地文件
EventLogStore::new(Path::new("/tmp/nexora-events")).await?
```

#### 生产
```rust
// MinIO 3 节点 HA 集群
let config = StorageConfig::S3 {
    endpoint: "http://minio-cluster:9000".into(),
    bucket: "nexora-events".into(),
    // ... MinIO 配置
};
EventLogStore::new_with_config(config).await?
```

---

## 📚 相关文档

1. [`docs/deployment/MINIO_DEPLOYMENT.md`](docs/deployment/MINIO_DEPLOYMENT.md) - MinIO 部署指南
2. [`docs/deployment/EVENT_STORE_NFS_SETUP.md`](docs/deployment/EVENT_STORE_NFS_SETUP.md) - NFS 设置指南
3. [`docs/architecture/EVENT_STORE_LOCAL_REPLICATION_DESIGN.md`](docs/architecture/EVENT_STORE_LOCAL_REPLICATION_DESIGN.md) - 技术设计分析
4. [`docs/testing/DISTRIBUTED_PGWIRE_TEST_REPORT.md`](docs/testing/DISTRIBUTED_PGWIRE_TEST_REPORT.md) - 测试报告

---

**生成时间**: 2026-07-21  
**分析范围**: `nexora-eventlog` + `nexora-pgwire` crates
