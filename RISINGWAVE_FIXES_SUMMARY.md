# RisingWave 编译修复总结

## 问题根源

RisingWave v3.0.2 在 Nexora 项目中编译失败的根本原因：

1. **错误的 prost 依赖** - 使用了 fork 而非官方版本
2. **错误的 Rust 工具链** - 使用了不兼容的 nightly 版本
3. **过期的生成文件** - protobuf 生成的 Rust 代码不匹配

## 修复步骤

### 1. 恢复官方 Cargo.toml

**文件**: `vendor/risingwave/Cargo.toml`

**错误配置**:
```toml
prost = { git = "https://github.com/risingwavelabs/prost.git", rev = "040a192..." }
```

**正确配置**:
```toml
prost = { version = "=0.14.3" }
prost-build = { version = "=0.14.3" }
prost-derive = { version = "=0.14.3" }
```

**原因**: RisingWave fork 的 prost 生成的类型名没有 `Pb` 前缀，导致 34+ 类型未找到错误。

### 2. 修正 Rust 工具链

**文件**: `vendor/risingwave/rust-toolchain.toml`

**错误配置**:
```toml
[toolchain]
channel = "nightly-2026-03-15"
```

**正确配置**:
```toml
[toolchain]
channel = "nightly-2025-10-10"
```

**原因**: nightly-2026-03-15 与 hashbrown 0.15.5 不兼容，导致 `cannot specialize on trait Copy` 错误。

**命令**:
```bash
rustup override set nightly-2025-10-10
export PATH="$HOME/.cargo/bin:$HOME/.rustup/toolchains/nightly-2025-10-10-aarch64-apple-darwin/bin:$PATH"
```

### 3. 重新生成 Protobuf 文件

**目录**: `vendor/risingwave/src/prost/src/`

**步骤**:
```bash
cd vendor/risingwave/src/prost/src

# 1. 删除所有生成的文件（保留 lib.rs 和 id.rs）
ls *.rs | grep -v "^lib.rs$" | grep -v "^id.rs$" | xargs rm -f

# 2. 清理并重新构建
cd ../../../
cargo clean -p risingwave_pb
cargo build -p risingwave_pb
```

**结果**: 56+ .rs 文件被正确生成，所有文件都包含 `#[derive(prost_helpers::AnyPB)]`

### 4. 恢复官方源文件

从官方 RisingWave v3.0.2 仓库恢复以下文件：

```bash
# 临时克隆官方仓库
git clone --depth 1 --branch v3.0.2 https://github.com/risingwavelabs/risingwave.git /tmp/risingwave-check

# 恢复关键文件
cp /tmp/risingwave-check/src/prost/src/lib.rs vendor/risingwave/src/prost/src/
cp /tmp/risingwave-check/src/prost/src/id.rs vendor/risingwave/src/prost/src/
cp /tmp/risingwave-check/src/common/src/types/interval.rs vendor/risingwave/src/common/src/types/
```

**恢复的文件**:
- `src/prost/src/lib.rs` - 修复 TypedId 转换错误（47 个错误）
- `src/prost/src/id.rs` - 修复 TypedId wrapper 定义
- `src/common/src/types/interval.rs` - 修复导入（使用 `PbInterval` 而非 `Interval`）

## 技术细节

### AnyPB 宏机制

RisingWave 使用自定义 proc macro 生成 `Pb` 前缀的类型别名：

**输入** (proto 文件):
```protobuf
message Interval {
  int32 months = 1;
  int32 days = 2;
  int64 microseconds = 3;
}
```

**prost 生成**:
```rust
#[derive(prost_helpers::AnyPB)]  // 关键：这个 derive
#[derive(Clone, Copy, PartialEq, ::prost::Message)]
pub struct Interval {
    #[prost(int32, tag = "1")]
    pub months: i32,
    // ...
}
```

**AnyPB 宏展开**:
```rust
// 宏生成的类型别名
pub type PbInterval = Interval;
```

**应用代码使用**:
```rust
use risingwave_pb::data::PbInterval;  // 使用 Pb 前缀的别名

// 避免与本地类型冲突
pub struct Interval { /* 应用层类型 */ }
```

### 为什么需要 Pb 前缀

1. **避免命名冲突**: protobuf 类型（`Interval`）vs 应用层类型（`Interval`）
2. **清晰的类型语义**: `PbInterval` 明确表示这是 protobuf 类型
3. **类型安全**: 区分序列化类型和业务逻辑类型

## 自动化脚本

创建了 `scripts/fix-risingwave-protobuf.sh` 脚本来自动化修复流程：

```bash
#!/bin/bash
# 一键修复 RisingWave protobuf 生成问题

./scripts/fix-risingwave-protobuf.sh
```

**功能**:
1. 验证 nightly-2025-10-10 工具链
2. 检查 Cargo.toml 中的 prost 版本
3. 清理生成的文件
4. 重新构建 risingwave_pb
5. 验证生成的文件包含 AnyPB derive

## 验证步骤

### 1. 验证 protobuf 生成

```bash
cd vendor/risingwave/src/prost/src
head -5 data.rs
```

**预期输出**:
```rust
// This file is @generated by prost-build.
#[derive(prost_helpers::AnyPB)]  // ← 必须有这一行
#[derive(Clone, Copy, PartialEq, Eq, Hash, ::prost::Message)]
pub struct Interval {
```

### 2. 验证工具链

```bash
rustc --version
```

**预期输出**:
```
rustc 1.92.0-nightly (b925a865e 2025-10-09)
```

### 3. 验证完整构建

```bash
export PATH="$HOME/.cargo/bin:$HOME/.rustup/toolchains/nightly-2025-10-10-aarch64-apple-darwin/bin:$PATH"
cargo build -p risingwave_cmd_all --bin risingwave
```

**预期**: 成功构建，生成 `target/debug/risingwave` 二进制文件

## 错误历史

### 错误 1: Pb* 类型未找到（34+ 错误）
```
error[E0433]: failed to resolve: use of undeclared type `PbArray`
error[E0433]: failed to resolve: use of undeclared type `PbDataChunk`
error[E0433]: failed to resolve: use of undeclared type `PbInterval`
```
**修复**: 恢复官方 Cargo.toml（prost 0.14.3）

### 错误 2: AnyPB derive 缺失（80+ 错误）
```
error[E0277]: `data::DataType` doesn't implement `std::fmt::Debug`
```
**修复**: 删除旧文件，重新构建 risingwave_pb

### 错误 3: TypedId 类型不匹配（47 错误）
```
error[E0308]: mismatched types
   --> src/prost/src/lib.rs:386:29
    |
386 |             worker_node_id: worker_node_id.0,
    |                             ^^^^^^^^^^^^^^^^ expected `TypedId<6, u32>`, found `u32`
```
**修复**: 恢复官方 lib.rs 和 id.rs

### 错误 4: hashbrown 特化错误
```
error: cannot specialize on trait `Copy`
    --> hashbrown-0.15.5/src/raw/mod.rs:3269:9
     |
3269 | impl<T: Copy, A: Allocator + Clone> RawTableClone for RawTable<T, A> {
     |         ^^^^
```
**修复**: 切换到 nightly-2025-10-10

### 错误 5: Orphan rule 错误（441 个错误）
```
error[E0117]: only traits defined in the current crate can be implemented for types defined outside of the crate
   --> src/common/src/catalog/column.rs:257:1
257 | impl From<ColumnDesc> for ColumnDesc {
    |      `risingwave_pb::plan_common::ColumnDesc` is not defined in the current crate
```
**根本原因**: `src/common/src/` 目录下的文件是过时的或被错误修改的版本
- 错误版本：`use risingwave_pb::plan_common::ColumnDesc;`（直接导入，与本地类型冲突）
- 正确版本：`use risingwave_pb::plan_common::PbColumnDesc;`（使用 Pb 前缀别名）

**修复**: 从官方 v3.0.2 恢复整个 `src/` 目录（除了 protobuf 生成文件）
```bash
# 恢复所有源文件，但保留我们正确生成的 protobuf 文件
rsync -av --delete --exclude 'prost/src/*.rs' /tmp/risingwave-check/src/ vendor/risingwave/src/
```

## 根本原因总结

**核心问题**: 我们的 RisingWave 代码是一个**混合版本**，包含：
1. ✅ 正确的 Cargo.toml（使用 prost fork）
2. ✅ 正确的 rust-toolchain.toml（nightly-2025-10-10）
3. ✅ 正确生成的 protobuf 文件（带 AnyPB derive）
4. ❌ **过时的应用层代码**（src/common/src/），不使用 Pb* 前缀

**解决方案**: 从官方 v3.0.2 仓库恢复所有应用层源文件。

## 当前状态

✅ 所有依赖问题已解决  
✅ Protobuf 文件正确生成  
✅ 工具链已正确配置  
✅ 应用层代码已从官方恢复  
🔄 正在重新构建完整的 RisingWave 二进制文件

## 下一步

1. 等待构建完成
2. 测试 RisingWave 二进制：`./target/debug/risingwave --help`
3. 开始 Phase 2：创建 nexora-risingwave 包装器 crate
4. 实现特性标志：`--features risingwave`
5. 集成到 Nexora 应用中

## 参考

- RisingWave 官方文档：https://docs.risingwave.com
- RisingWave v3.0.2 源代码：https://github.com/risingwavelabs/risingwave/tree/v3.0.2
- prost 文档：https://docs.rs/prost/0.14.3
