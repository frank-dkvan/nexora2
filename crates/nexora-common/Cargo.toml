[package]
name = "nexora-common"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.40", features = ["time", "macros", "rt-multi-thread"] }
tracing = "0.1"
rand = "0.8"
anyhow = "1.0"

[dev-dependencies]
tokio = { version = "1.40", features = ["full"] }
